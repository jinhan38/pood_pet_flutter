import 'package:flutter/material.dart';

class BaseBodyStateless extends StatelessWidget {
  const BaseBodyStateless({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
